% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qhat_generation.R
\name{qhat_generation}
\alias{qhat_generation}
\title{Generate nuisance function estimates for one train/test split}
\usage{
qhat_generation(
  train,
  test,
  j,
  k,
  capture_names,
  nuisance_function = c("glm", "gam", "random_forest"),
  margin = 0.005,
  ...
)
}
\arguments{
\item{train}{Training data frame with capture columns followed by covariates.}

\item{test}{Test data frame with the same columns as train.}

\item{nuisance_function}{One of "logit", "gam", or "ranger".}

\item{margin}{Numeric threshold used for overlap warnings and nuisance
truncation. Default is 0.005.}

\item{...}{Additional arguments passed to the qhat_* function.}

\item{capture_indicators}{Character vector of capture column names, or
integer vector of column indices (1-indexed).}
}
\value{
A nested list keyed by pair names. Each element contains a list with
  q1, q2, and q12 predictions for the rows in test.
}
\description{
Fits a single nuisance function on a training split and predicts capture
probabilities on a test split for all pairwise capture-list combinations.
}
\keyword{internal}
